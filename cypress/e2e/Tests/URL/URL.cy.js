/*class URLs 
{
    static baseUrlForUI = "https://parabank.parasoft.com/parabank/index.htm?ConnType=JDBC";

    static baseUrlForAPI = 'https://reqres.in/'

    static authenticationURL = `${this.baseUrlForAPI}/api/login`;
    static validatePaginationURL1 = `${this.baseUrlForAPI}/api/users/1`
    static validatePaginationURL2 = `${this.baseUrlForAPI}/api/users/2`;
    static userCreationURL = `${this.baseUrlForAPI}/api/users`;
    static getSpecificUserDataURL = (id) => `${this.baseUrlForAPI}/users/${id}`;
    
    
  }
  export default URLs;

  Page object model can be used if it required. Here you can find the url's we are working on
  and each can be imported to other test classes to create an object and use.
  */